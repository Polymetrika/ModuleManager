@page
@model ModuleManager.Pages.LearningContents.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>LearningContent</h4>
    <hr />
        <div class="col-sm-2">
            @Html.DisplayNameFor(model => model.LearningContent.TemplateId) : 
            @Html.DisplayFor(model => model.LearningContent.TemplateId)
        </div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.LearningContent.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.LearningContent.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.LearningContent.Details)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.LearningContent.Details)
        </dd>
        <!-- <snippet> -->
        @*Precedng markup omitted for brevity.*@
    <dt>
            @Html.DisplayNameFor(model => model.LearningContent.Status)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.LearningContent.Status)
        </dd>
    </dl>
</div>

        
            <button type="submit" class="btn btn-xs btn-primary">LearningContent</button>
@if (Model.LearningContent.Status != LearningContentStatus.Approved)
{
    @if ((await AuthorizationService.AuthorizeAsync(
     User, Model.LearningContent, ModuleOperations.Approve)).Succeeded)
    {
        <form style="display:inline;" method="post">
            <input type="hidden" name="id" value="@Model.LearningContent.LearningContentId" />
            <input type="hidden" name="status" value="@LearningContentStatus.Approved" />
            <button type="submit" class="btn btn-xs btn-success">Approve</button>
        </form>
    }
}

@if (Model.LearningContent.Status != LearningContentStatus.Rejected)
{
    @if ((await AuthorizationService.AuthorizeAsync(
     User, Model.LearningContent, ModuleOperations.Reject)).Succeeded)
    {
        <form style="display:inline;" method="post">
            <input type="hidden" name="id" value="@Model.LearningContent.LearningContentId" />
            <input type="hidden" name="status" value="@LearningContentStatus.Rejected" />
            <button type="submit" class="btn btn-xs btn-danger">Reject</button>
        </form>
    }
}

<div>
    @if ((await AuthorizationService.AuthorizeAsync(
         User, Model.LearningContent,
         ModuleOperations.Update)).Succeeded)
    {
        <a asp-page="./Edit" asp-route-id="@Model.LearningContent.LearningContentId">Edit</a>
        <text> | </text>
    }
    <a asp-page="./Index">Back to List</a>
</div>
<!-- </snippet> -->
