@page
@model ModuleManager.Pages.Processes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>BusinessProcess Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BusinessProcess[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BusinessProcess[0].ReleaseStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BusinessProcess) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseStatus)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ProcessId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ProcessId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ProcessId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
